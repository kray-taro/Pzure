/**
 * Web entrypoint placeholder (Phase 1). The offline-first PWA shell (ADR-006)
 * and design system arrive in Module 10 (#11). Present so the project has a
 * lintable source file and a clear mount point.
 */
export function mount(): void {
  // Phase 1 seam only.
}
